import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
from matplotlib import cm
from mpl_toolkits.mplot3d.axes3d import get_test_data




#zad1
# z = np.arange(0, np.pi*2, .1)
# plt.plot(z, np.sin(z), 'b', label='sin(z)')
# plt.plot(z, np.cos(z)*2, 'r', label='2cos(z)')
# plt.xlabel('z')
# plt.ylabel('sin(z) cos(z)')
# plt.legend(loc='upper right')
# plt.title('Wykres sin(z), 2cos(z)')
# plt.show()